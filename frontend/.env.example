# Оставьте пустым, если фронтенд и API находятся на одном домене (reverse proxy на /dogs, /calendar, /uploads).
VITE_API_BASE_URL=

# Куда проксировать в dev (npm run dev). По умолчанию http://127.0.0.1:8000
VITE_DEV_API_PROXY_TARGET=http://127.0.0.1:8000
