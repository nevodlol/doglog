# PostgreSQL (обязательно на проде)
# DATABASE_URL=postgresql+psycopg://user:pass@host:5432/dbname

# Разрешённые Origin для CORS (через запятую, без пробелов внутри URL или с trim)
# CORS_ORIGINS=https://doglog.example.com,https://www.doglog.example.com
